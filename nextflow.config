// nextflow.config
outputDir = 'results'

profiles {
    local {
        process {
            executor = 'local'
        }
    }

    hpc {
        executor {
            name = 'slurm'
        }
    }
}

singularity {
    enabled    = true
    autoMounts = true
    cacheDir   = "${launchDir}/singularity-cache"
}

process {
    cache = 'lenient'
    errorStrategy =  'finish'

    withName: 'softmask' {
        container      = 'bedtools.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 8.GB
        time           = 2.h
        cpus           = 1
    }

    withName: 'helixer' {
        container        = 'helixer.sif'
        containerOptions = '--nv'
        queue            = 'jic-gpu,jic-informatics'
        memory           = 100.GB
        time             = 2.d
        cpus             = 1
        clusterOptions   = '--gres=gpu:1'
    }

    withName: 'annevo' {
        container        = 'annevo-fix.sif'
        containerOptions = '--nv'
        queue            = 'jic-gpu,jic-informatics'
        memory           = 950.GB
        time             = 2.d
        cpus             = 32
        clusterOptions   = '--gres=gpu:1 --exclude=j1024n5'
    }

    withName: 'tiberius' {
        container        = 'tiberius-2.0.6.sif'
        containerOptions = '--nv'
        queue            = 'jic-gpu,jic-informatics'
        memory           = 400.GB
        time             = 2.d
        cpus             = 1
        clusterOptions = '--gres=gpu:1'
    }

    withName: 'miniprot_alignment' {
        container      = 'miniprot.sif'
        queue          = 'jic-compute'
        memory         = 300.GB
        time           = 7.d
        cpus           = 64
    }

    withName: 'star_index' {
        container      = 'star.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 100.GB
        time           = 1.d
        cpus           = 4
    }

    withName: 'trim_rnaseq_data' {
        container      = 'cutadapt.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 4.GB
        time           = 1.d
        cpus           = 12
    }

    withName: 'star_map_rnaseq_data_first_pass' {
        container      = 'star.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 60.GB
        time           = 1.d
        cpus           = 12
    }

    withName: 'star_map_rnaseq_data_second_pass' {
        container      = 'star.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 80.GB
        time           = 1.d
        cpus           = 12
    }

    withName: 'braker3' {
        container      = 'braker3.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 256.GB
        time           = 7.d
        cpus           = 8
    }

    withName: 'stringtie_assemble' {
        container      = 'stringtie.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 32.GB
        time           = 1.d
        cpus           = 4
    }
    
    withName: 'ingenannot_validate_annotation' {
        container      = 'ingenannot.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 16.GB
        time           = 1.d
        cpus           = 1
    }

    withName: 'sort_bgzip_index_miniprot_mappings' {
        container      = 'samtools.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 8.GB
        time           = 1.d
        cpus           = 8
    }

    withName: 'combine_stringtie_transcripts' {
        container      = 'stringtie.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 2.GB
        time           = 1.d
        cpus           = 1
    }

    withName: 'sort_bgzip_index_combined_stringtie_transcript' {
        container      = 'samtools.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 8.GB
        time           = 1.d
        cpus           = 8
    }

    withName: 'create_masked_fasta_minimap_index'  {
        container      = 'pacbio_tools.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 64.GB
        time           = 1.d
        cpus           = 1
    }

    withName: 'convert_cram_flnc_to_bam'  {
        container      = 'samtools.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 8.GB
        time           = 1.d
        cpus           = 1
    }

    withName: 'align_isoseq_reads_to_genome'  {
        container      = 'pacbio_tools.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 64.GB
        time           = 1.d
        cpus           = 8
    }

    withName: 'collapse_isoseq'  {
        container      = 'pacbio_tools.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 8.GB
        time           = 1.d
        cpus           = 1
    }

    withName: 'samtools_index' {
        container      = 'samtools.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 2.GB
        time           = 1.d
        cpus           = 1
    }

    withName: 'isoform_ranking' {
        container      = 'ingenannot.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 256.GB
        time           = 1.d
        cpus           = 1
    }

    withName: 'sort_bgzip_index_top_isoforms' {
        container      = 'samtools.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 8.GB
        time           = 1.d
        cpus           = 8
    }

    withName: 'compute_aed_score_for_annotation' {
        container      = 'ingenannot.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 64.GB
        time           = 2.d
        cpus           = 8
    }

    withName: 'ingenannot_selection_process' {
        container      = 'ingenannot.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 64.GB
        time           = 1.d
        cpus           = 8
    }

    withName: 'ingenannot_compare' {
        container      = 'ingenannot.sif'
        queue          = 'jic-compute,nbi-compute,jic-informatics'
        memory         = 64.GB
        time           = 1.d
        cpus           = 8
    }
}
